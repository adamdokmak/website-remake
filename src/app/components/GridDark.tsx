import { motion } from "framer-motion";
export default function GridDark({ isOpen }: { isOpen: boolean }) {
    const pathVariants = {
        initial: {
            width: '0%',
        },
        animate: {
            width: '100%',
            transition: {
                duration: 0.5,
            }
        },
    }
  return (
    <>
    </>
  );
}
